interface:
  display_name: "CodeStable Do"
  short_description: "读取任务上下文，执行 ready 工作，复用现有实现并记录证据"
  default_prompt: "Use $cs-do to execute ready CodeStable work, read the task context pack, apply the minimality ladder, and record evidence."
